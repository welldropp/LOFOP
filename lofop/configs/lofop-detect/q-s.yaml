# NMS-free small: the reference query variant.
extends: [q-base.yaml]

model:
  backbone:
    widths: [48, 96, 192, 384]
    depths: [1, 2, 4, 2]
  neck:
    in_channels: [96, 192, 384]
    width: 96
  head:
    width: 96
    feedforward: 256
