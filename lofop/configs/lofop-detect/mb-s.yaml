# SwiftNet small: wider inverted-residual backbone on the dense ApexHead.
extends: [base.yaml]

model:
  backbone:
    type: backbone/SwiftNet
    widths: [32, 64, 128, 256]
    depths: [2, 3, 4, 2]
    expansion: 4.0
  neck:
    in_channels: [64, 128, 256]
    width: 96
  head:
    width: 96
